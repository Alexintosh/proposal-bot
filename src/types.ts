export interface Proposal {
    id: string;
    title: string;
    start: number;
    end: number;
    author: string;
    snapshot: string;
    state: string;
    description: string;
    image: string;
  }